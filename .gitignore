node_modules/
dist/
.turbo/
.vite/
.cache/

# Env
.env
.env.*.local

# OS
.DS_Store

# Logs
*.log
logs/

# Editor
.vscode/
.idea/
.antigravitycli

# Bun — commit `bun.lock` (text) for reproducible installs; ignore legacy binary lockfile.
bun.lockb

# Certs — whitelist only the CA bundle, ignore everything else (private keys, etc.)
certs/*
!certs/ca.crt
workspace/*
!workspace/.gitkeep

# Kimi share dir (config.toml, oauth, sessions, logs) — see KIMI_SHARE_DIR
.kimi/
.runtime/
claude-config/
agent-state/

reference/
playwright-report/
test-results/