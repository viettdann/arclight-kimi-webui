// Single source of truth for the running service version. Surfaced via the
// /health endpoint and forwarded to the Kimi SDK as `clientInfo.version`.
export const SERVICE_NAME = 'arclight-kimi-webui';
export const SERVICE_VERSION = '0.0.0';
